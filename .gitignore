# --- Secrets: never commit ---
# Local integration credentials and tokens must stay outside Git history.
*.env
.env
.env.*
!.env.example
!.env.sample
!.env.template
notion-sync-experiment/
secrets/
credentials/
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
credentials*.json
client_secret*.json
service-account*.json
service_account*.json
google-token*.json
auth-token*.json
tokens*.json

# --- Build output ---
# app/ is the published, ready-to-run build (dotnet publish target). Ship it as a
# GitHub Release attachment instead of committing binaries into the tree.
app/
app-pending/
app-test/
app-backup-*/
archive/generated/
dist/
bin/
obj/
*.user
NativeWidget/*_wpftmp.csproj

# --- Legacy Electron prototype ---
# Superseded by the WPF app in NativeWidget/ (see ARCHITECTURE.md "Removed features").
# Kept on disk locally for reference, not part of this repo.
legacy/electron-prototype/
node_modules/
package.json
package-lock.json
main.js
preload.js
renderer.js
config.js
index.html
style.css
oauth-google.js
oauth-helper.js
oauth-microsoft.js

# --- Local junk ---
run.log
urls.txt
*.log
.claude/
