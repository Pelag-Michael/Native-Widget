<Window x:Class="NativeWidget.LabelsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NativeWidget"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Labels" Width="300" Height="400" MinWidth="250" MinHeight="220"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize"
        Left="1090" Top="480" Closing="Window_Closing">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0"/>
    </shell:WindowChrome.WindowChrome>
    <Border Background="{StaticResource PanelBg}" CornerRadius="18" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="14" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" MouseLeftButtonDown="DragBar_MouseDown" Cursor="SizeAll" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE8EC;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="White" Margin="0,0,8,0"/>
                    <TextBlock Text="Labels" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="CountText" Foreground="{StaticResource MutedBrush}" FontSize="10.5" Margin="8,1,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="&#xE710;" ToolTip="Create label" Click="AddLabel_Click" Style="{StaticResource IconBtnStyle}"/>
                    <local:WidgetHeaderControls x:Name="HeaderControls"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="{StaticResource ChipBg}" CornerRadius="10" Padding="10,7" Margin="0,0,0,8">
                <TextBlock Text="Create labels here, then use the label icon in Notes, Tasks, or Calendar to tag an item."
                           Foreground="{StaticResource MutedBrush}" FontSize="10.5" TextWrapping="Wrap"/>
            </Border>

            <Grid Grid.Row="2">
                <TextBlock x:Name="EmptyHint" Text="No labels yet. Tap + to create one." Foreground="{StaticResource MutedBrush}"
                           FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                <ListBox x:Name="LabelsList" Background="Transparent" BorderThickness="0"
                         ScrollViewer.CanContentScroll="False" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VirtualizingPanel.IsVirtualizing="False" ItemContainerStyle="{StaticResource ListItemStyle}"/>
            </Grid>
        </Grid>
    </Border>
</Window>
