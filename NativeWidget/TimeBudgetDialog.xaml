<Window x:Class="NativeWidget.TimeBudgetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="320" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">
    <Border Background="#F2131217" CornerRadius="16" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="16">
        <StackPanel>
            <TextBlock x:Name="DialogTitle" Text="New time target" Foreground="White" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>

            <!-- Goal Name -->
            <TextBlock Text="Goal / Activity name" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,0,4"/>
            <TextBox x:Name="NameInput" Style="{StaticResource InputStyle}" Margin="0,0,0,10"/>

            <!-- Target Hours -->
            <TextBlock Text="Target hours to log" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,0,4"/>
            <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="HoursInput" Text="8" Style="{StaticResource InputStyle}" Margin="0,0,6,0"/>
                <TextBlock Grid.Column="1" Text="hours" Foreground="{StaticResource MutedBrush}" FontSize="12" VerticalAlignment="Center"/>
            </Grid>

            <WrapPanel Margin="0,0,0,10">
                <Button Content="4h" Click="HoursPreset_Click" Tag="4" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="8h" Click="HoursPreset_Click" Tag="8" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="10h" Click="HoursPreset_Click" Tag="10" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="15h" Click="HoursPreset_Click" Tag="15" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="20h" Click="HoursPreset_Click" Tag="20" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="40h" Click="HoursPreset_Click" Tag="40" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
            </WrapPanel>

            <!-- Time Frame (In how many days) -->
            <TextBlock Text="Timeframe duration" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,0,4"/>
            <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="DaysInput" Text="7" Style="{StaticResource InputStyle}" Margin="0,0,6,0"/>
                <TextBlock Grid.Column="1" Text="days" Foreground="{StaticResource MutedBrush}" FontSize="12" VerticalAlignment="Center"/>
            </Grid>

            <WrapPanel Margin="0,0,0,10">
                <Button Content="1 day (Daily)" Click="DaysPreset_Click" Tag="1" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="7 days (1 week)" Click="DaysPreset_Click" Tag="7" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="14 days (2 weeks)" Click="DaysPreset_Click" Tag="14" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
                <Button Content="30 days (1 month)" Click="DaysPreset_Click" Tag="30" Style="{StaticResource SecondaryBtnStyle}" Padding="7,3" Margin="0,0,4,4"/>
            </WrapPanel>

            <!-- Repeat option -->
            <Border Background="{StaticResource ChipBg}" CornerRadius="9" Padding="10,8" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox x:Name="RepeatCheck" IsChecked="True" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text="Repeat automatically when duration ends" Foreground="White" FontSize="11.5" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Action buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                <Button Content="Cancel" Click="Cancel_Click" Style="{StaticResource SecondaryBtnStyle}" Width="70" Margin="0,0,8,0"/>
                <Button Content="Save" Click="Ok_Click" Style="{StaticResource PrimaryBtnStyle}" Width="70"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
