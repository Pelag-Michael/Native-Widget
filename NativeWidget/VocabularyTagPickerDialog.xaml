<Window x:Class="NativeWidget.VocabularyTagPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="300" SizeToContent="Height" MaxHeight="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="TagChoiceStyle" TargetType="ToggleButton">
            <Setter Property="Foreground" Value="#B9B8C1"/>
            <Setter Property="Background" Value="#16FFFFFF"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="TagBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="11" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="TagBorder" Property="Background" Value="#24FFFFFF"/></Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="#DDE7FF"/>
                                <Setter TargetName="TagBorder" Property="Background" Value="#284A7DFF"/>
                                <Setter TargetName="TagBorder" Property="BorderBrush" Value="#667FA8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="#FA131217" CornerRadius="16" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="14">
        <StackPanel>
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Vocabulary tags" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="Used only in the vocabulary notebook" Foreground="{StaticResource MutedBrush}" FontSize="9.5" Margin="0,2,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="&#xE710;" FontFamily="Segoe MDL2 Assets" ToolTip="Create tag"
                        Click="AddTag_Click" Style="{StaticResource IconBtnStyle}"/>
            </Grid>
            <TextBlock x:Name="EmptyHint" Text="No tags yet. Click + to create one." Foreground="{StaticResource MutedBrush}"
                       FontSize="11" Margin="0,5,0,10" Visibility="Collapsed"/>
            <ScrollViewer MaxHeight="250" VerticalScrollBarVisibility="Auto">
                <WrapPanel x:Name="TagsPanel"/>
            </ScrollViewer>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Cancel" Click="Cancel_Click" Style="{StaticResource SecondaryBtnStyle}" Padding="11,5" Margin="0,0,7,0"/>
                <Button Content="Apply" Click="Save_Click" Style="{StaticResource PrimaryBtnStyle}" Padding="11,5"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
