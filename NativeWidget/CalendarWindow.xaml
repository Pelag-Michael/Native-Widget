<Window x:Class="NativeWidget.CalendarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NativeWidget"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Calendar" Width="300" Height="420" MinHeight="200" MinWidth="240"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize"
        Left="90" Top="40" Closing="Window_Closing">
    <!-- WindowStyle="None" windows can normally only be resized via the corner grip -
         WindowChrome adds real edge/corner hit-testing (drag any side, not just the
         bottom-right corner) without needing a native title bar. -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0"/>
    </shell:WindowChrome.WindowChrome>
    <Border Background="{StaticResource PanelBg}" CornerRadius="18" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" Padding="14" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" MouseLeftButtonDown="DragBar_MouseDown" Cursor="SizeAll" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="White" Margin="0,0,8,0"/>
                    <TextBlock Text="Calendar" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="CalendarStatus" Text="Not connected" Foreground="{StaticResource MutedBrush}" FontSize="10.5" Margin="8,1,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="AddEventBtn" Content="&#xE710;" ToolTip="New event" Click="AddEvent_Click" Style="{StaticResource IconBtnStyle}" Visibility="Collapsed"/>
                    <Button x:Name="RefreshBtn" Content="&#xE72C;" ToolTip="Refresh calendar" Click="RefreshBtn_Click" Style="{StaticResource IconBtnStyle}" Visibility="Collapsed"/>
                    <Button x:Name="GoogleDisconnectBtn" Content="&#xE711;" ToolTip="Disconnect Google" Click="GoogleDisconnectBtn_Click" Style="{StaticResource IconBtnStyle}" Visibility="Collapsed"/>
                    <local:WidgetHeaderControls x:Name="HeaderControls"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <StackPanel x:Name="CalDisconnected">
                    <TextBlock x:Name="DisconnectedMessage" Text="Not connected to Google Calendar." Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,4,0,10" TextWrapping="Wrap"/>
                    <Button x:Name="GoogleConnectBtn" Content="Connect Google Calendar" Click="GoogleConnectBtn_Click" Style="{StaticResource PrimaryBtnStyle}" HorizontalAlignment="Left"/>
                </StackPanel>
                <Grid x:Name="CalConnected" Visibility="Collapsed">
                    <ListBox x:Name="EventList" Background="Transparent" BorderThickness="0"
                             ScrollViewer.CanContentScroll="False"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             VirtualizingPanel.IsVirtualizing="False"
                             ItemContainerStyle="{StaticResource ListItemStyle}" MouseUp="EventList_MouseUp"/>
                    <TextBlock x:Name="LoadingHint" Text="Loading..." Foreground="{StaticResource MutedBrush}"
                               FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
